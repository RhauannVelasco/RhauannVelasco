name: Generate Snake

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  generate:
    permissions:
      contents: write
    runs-on: ubuntu-latest

    steps:
      - name: Generate neon blue snake svg
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: RhauannVelasco
          outputs: |
            dist/github-contribution-grid-snake-neon.svg?color_snake=%2300E5FF&color_dots=%230D1117,%230A1A2F,%23003B73,%230006FF,%2300E5FF
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Push snake to output branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
